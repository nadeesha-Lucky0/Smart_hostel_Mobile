{
  "name": "smart-hostel-management-system",
  "version": "1.0.0",
  "description": "Smart Hostel Management System root package",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm start --prefix mobile\"",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix mobile && npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "mobile": "npm start --prefix mobile"
  },
  "devDependencies": {
    "@types/react": "~19.2.10",
    "concurrently": "^9.1.2",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "expo": "^55.0.19"
  }
}
