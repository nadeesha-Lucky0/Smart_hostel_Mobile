import { Stack } from 'expo-router';

/**
 * Authentication Layout 
 * Handles login and registration screens.
 */
export default function AuthLayoutBody() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
